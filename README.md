Creating a new page:

```bash
./new.sh <page>
```

Building a page:

```bash
cd <page>
./build.sh
```

Serving a page:

```bash
cd <page>
./serve.sh
```

Remove / clear build / serve cache:

```bash
cd <repository root>
rm -rf cache
```

